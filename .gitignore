node_modules/
dist/
.next/
package-lock.json
tsconfig.tsbuildinfo
.partykit/
.env
.env.local
.env*.local
.env.sentry-build-plugin
storybook-static
next-env.d.ts

# Terraform — state and working directory are machine-local
# (terraform/.gitignore covers the terraform/ subdirectory;
# these patterns catch accidental state files placed at the root)
*.tfstate
*.tfstate.backup
